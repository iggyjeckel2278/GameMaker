//detemine if we're standing
if ((!leftKey && !rightKey) || (leftKey && rightKey)) && (!jumping && !falling)
{
    state = "standing";
       hspeed =0;
        vspeed =0;
        hspeed = 0;
        vspeed = 0;
        image_speed = 0;
    
}

